Read ../AGENTS.md and ../docs/INSTALL.md. This is the bundled SmellyType plugin,
not a Voxtype extension. Preserve running dictation and user configuration.
Use the installed Omarchy skill for desktop changes. Never edit /usr/share/omarchy.
Do not handle private keys in QML or command arguments. credentials.py is the
terminal-only key editor. Verify the distinct SmellyType Configuration menu entry.
