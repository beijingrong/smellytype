# SmellyType autostart for Sway
# Place in /etc/sway/config.d/ or ~/.config/sway/config.d/
#
# Starts smellytype daemon on login if installed

exec_always --no-startup-id systemctl --user start smellytype.service
